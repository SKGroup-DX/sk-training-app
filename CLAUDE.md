\# SKグループ 研修管理アプリ(きょうたのP) - プロジェクト概要



\## 概要

中途採用者向け研修管理アプリ。17週間の週次プログラム(index.htmlのTRAININGS配列、id 1〜17)の進捗管理、面談担当者の割り当てを行う。各週は自己紹介・挨拶・身だしなみ・清掃・企業理念経営方針・クレドカード・振り返りの7つの大テーマに整理されている。

URL: https://kasuyakouta.github.io/sk-training-app/



\## 技術スタック

\- フロント: GitHub Pages上の単一HTMLファイル(1,714行)

\- バックエンド: Google Apps Script (GAS)

\- スプレッドシートID: 1Y8geetQ12t2NSeAyKP56VBKtFHe\_-B2Vzm\_MaejYjIE

\- シート構成: 受講者マスタ・研修マスタ・受講記録・スケジュール・面談記録・面談者マスタ・担当設定・システム設定



\## このアプリ特有の設計(他アプリと異なる点に注意)

\- \*\*管理者PINは3150\*\*(他アプリと共通)。index.html:1626のADMIN\_PINで判定し、ヘッダーの編集ボタンからPIN認証モーダルを呼び出してbody.adminクラスを切り替える

\- GAS URLはlocalStorage(キー'sk\_gas')で管理し、未設定時は「gas-banner」でURL入力を促す

\- GASデプロイ設定は「実行ユーザー:自分/アクセス:全員」。\*\*コード更新時は既存デプロイの編集ではなく「新しいデプロイ」の作成が必須\*\*(GAS側コード冒頭にも明記あり)

\- 受講者追加時、研修1〜17の受講記録が自動で初期化される(pending状態)

\- GAS通信失敗時はlocalStorageにフォールバックし、後から同期する設計。書き込み(apiPost)が失敗した場合はlocalStorage(キー'sk\_pending')にキューとして保存し、次回の通信成功時(30秒ごとの自動更新・オンライン復帰時)に自動再送する(index.htmlのflushPendingQueue)。未送信のキューが残っている間は、自動更新によるサーバーデータでの上書き(画面反映)を見送り、ローカルの変更を保護する



\## 必須ルール(標準スタック)

\- iOS Safari互換性・PWA対応を優先の設計制約とする

\- 日時はローカル時刻で組み立てる(UTCは使わない)



\## 変更時のお願い

\- 複雑な変更は実装前にオプションA/B形式で提案し、承認を得てから実装する

\- 回答は簡潔に、前置きは省略する

\- \*\*GAS側コードを変更した場合は「新しいデプロイ」が必要である旨を必ず案内すること\*\*

